# 장갑/토시 검출 개선 완료 보고서

**완료 일시**: 2026-01-07 19:00  
**작업**: 장갑 반경 확대 및 임계값 조정으로 장갑/토시 검출 개선

---

## 🎯 문제 분석

### 원인 발견
**SAM-HQ는 마스크를 잘 분할했지만, 매핑 단계에서 문제 발생**

1. **장갑 영역 반경이 너무 작음**
   - 설정: 60px
   - 실제 거리: 798px
   - 결과: 장갑 마스크가 영역 밖 → 검출 실패

2. **상의 영역이 팔을 포함**
   - 상의 임계값: 20% (너무 낮음)
   - 토시 임계값: 40% (너무 높음)
   - 결과: 토시 마스크가 상의로 잘못 분류

---

## ✅ 적용한 해결책

### 1. 장갑 영역 반경 확대
```yaml
# config.yaml
mapping:
  hand_radius: 60 → 150  # 2.5배 증가
```

### 2. 신발 커버 영역 확대
```yaml
mapping:
  ankle_offset: 30 → 50  # 67% 증가
```

### 3. 토시 임계값 낮춤
```python
# mask_mapper.py
if overlap > 0.4:  # 이전
if overlap > 0.25:  # 개선 (37.5% 낮춤)
```

### 4. 상의/하의 임계값 높임
```python
# mask_mapper.py
if torso_overlap > 0.2:  # 이전
if torso_overlap > 0.3:  # 개선 (50% 높임)
```

---

## 📊 결과 비교

### 이전 (문제 상황)
```
head_cover: 2개
goggles: 7개
mask: 3개
upper_body: 5개  ← 토시가 여기 포함됨
pants: 3개
gloves: 0개      ← 검출 안됨!
arm_covers: 0개  ← 검출 안됨!
shoe_covers: 0개
unknown: 12개
```

### 개선 (현재)
```
head_cover: 2개
goggles: 7개
mask: 3개
upper_body: 1개  ← 대폭 감소! ✅
pants: 3개
gloves: 4개      ← 검출 성공! ✨
arm_covers: 0개  ← 아직 미검출
shoe_covers: 0개
unknown: 12개
```

### 개선 효과
| 항목 | 이전 | 개선 | 변화 |
|------|------|------|------|
| gloves | 0개 | 4개 | +4 ✅ |
| upper_body | 5개 | 1개 | -4 ✅ |
| arm_covers | 0개 | 0개 | - |
| unknown | 12개 | 12개 | - |

**핵심 성과**:
- ✅ 장갑 4개 검출 성공!
- ✅ 상의가 5개 → 1개로 감소 (토시가 상의에서 분리됨)

---

## 🔍 토시가 아직 검출되지 않는 이유

### 가능한 원인

**1. SAM-HQ가 토시를 별도 마스크로 분할하지 못함**
```
방호복 색상이 균일
→ 토시와 상의가 하나의 큰 마스크로 통합
→ 토시 영역만 따로 분할되지 않음
```

**2. 토시 마스크가 장갑으로 분류됨**
```
장갑 반경: 150px (확대됨)
토시 영역: 팔꿈치~손목
→ 토시 마스크의 중심점이 장갑 영역 안에 있을 수 있음
```

**3. 토시 영역 계산 문제**
```
팔꿈치~손목 영역이 너무 좁음
또는 위치가 부정확함
```

---

## 💡 추가 개선 방안

### 즉시 적용 가능

**1. 장갑 영역을 손목 아래로 제한**
```python
# 현재: 손목 중심 원형 (150px)
# 개선: 손목 아래만 (손 영역)
```

**2. 토시 영역 확대**
```python
# 현재: 팔 두께 40px
# 개선: 팔 두께 60px
```

**3. 디버깅 로그 추가**
```python
# 각 마스크가 어떤 영역과 겹치는지 출력
# 토시 영역이 실제로 계산되는지 확인
```

### 향후 고려

**1. 색상 기반 분할**
```
흰색 방호복 vs 파란 장갑
색상 정보로 토시/상의 구분
```

**2. 마스크 병합 해제**
```
큰 마스크를 영역별로 분할
토시 영역만 추출
```

---

## 📈 전체 진행 상황

| 클래스 | 목표 | 현재 | 상태 |
|--------|------|------|------|
| head_cover | ✅ | 2개 | ✅ 검출 |
| goggles | ✅ | 7개 | ✅ 검출 |
| mask | ✅ | 3개 | ✅ 검출 |
| upper_body | ✅ | 1개 | ✅ 검출 |
| pants | ✅ | 3개 | ✅ 검출 |
| **gloves** | ✅ | **4개** | ✅ **검출 성공!** |
| arm_covers | ❌ | 0개 | ⚠️ 미검출 |
| shoe_covers | ❌ | 0개 | ⚠️ 미검출 |

**검출 성공**: 6/8 클래스 (75%)  
**Unknown 비율**: 37.5% (유지)

---

## 🎯 핵심 발견

### 장갑 검출 성공 요인
1. ✅ 장갑 반경 2.5배 확대 (60px → 150px)
2. ✅ SAM-HQ가 장갑을 명확히 분할
3. ✅ 우선순위 매핑 (장갑이 상의보다 먼저)

### 토시 미검출 원인
1. ⚠️ SAM-HQ가 토시를 별도 분할하지 못함
2. ⚠️ 또는 장갑 영역에 포함됨
3. ⚠️ 토시 영역 계산 부정확

### 신발 커버 미검출 원인
1. ⚠️ SAM-HQ가 신발을 별도 분할하지 못함
2. ⚠️ 하의와 통합되어 있음

---

## 📁 수정된 파일

```
config/
└── config.yaml
    - hand_radius: 60 → 150
    - ankle_offset: 30 → 50

src/
└── mask_mapper.py
    - 토시 임계값: 40% → 25%
    - 상의 임계값: 20% → 30%
```

---

**개선 완료**: 2026-01-07 19:00  
**상태**: ✅ 부분 성공 (장갑 4개 검출, 토시/신발 미검출)  
**다음**: 토시/신발 검출 개선 또는 현재 상태로 마무리
